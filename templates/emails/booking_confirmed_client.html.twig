<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Confirmation de réservation</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            line-height: 1.6;
            color: #333;
            max-width: 600px;
            margin: 0 auto;
            padding: 20px;
            background-color: #f4f4f4;
        }
        .container {
            background-color: white;
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0,0,0,0.1);
        }
        .header {
            text-align: center;
            margin-bottom: 30px;
            color: #2c3e50;
        }
        .booking-details {
            background-color: #f8f9fa;
            padding: 20px;
            border-radius: 5px;
            margin: 20px 0;
            border-left: 4px solid #3498db;
        }
        .salon-info {
            background-color: #e8f4fd;
            padding: 15px;
            border-radius: 5px;
            margin: 20px 0;
        }
        .footer {
            margin-top: 30px;
            padding-top: 20px;
            border-top: 1px solid #eee;
            text-align: center;
            color: #666;
            font-size: 14px;
        }
        .highlight {
            color: #3498db;
            font-weight: bold;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>✅ Confirmation de réservation</h1>
            <p>Bonjour {{ client.firstName }} {{ client.lastName }},</p>
        </div>

        <p>Votre réservation a été confirmée avec succès ! Nous sommes ravis de vous accueillir.</p>

        <div class="booking-details">
            <h3>📅 Détails de votre rendez-vous</h3>
            <p><strong>Salon :</strong> {{ salon.name }}</p>
            <p><strong>Coiffeur :</strong> {{ stylist.user.firstName }} {{ stylist.user.lastName }}</p>
            <p><strong>Service :</strong> {{ service.name }}</p>
            <p><strong>Date et heure :</strong> {{ startAt|date('d/m/Y H:i') }}</p>
            <p><strong>Durée :</strong> {{ service.durationMinutes }} minutes</p>
            <p><strong>Fin prévue :</strong> {{ endAt|date('H:i') }}</p>
        </div>

        <div class="salon-info">
            <h3>📍 Informations du salon</h3>
            <p><strong>Adresse :</strong> {{ salon.address }}, {{ salon.city }}</p>
            <p><strong>Téléphone :</strong> {{ salon.owner.phone ?: 'Non spécifié' }}</p>
        </div>

        <div style="background-color: #d4edda; padding: 15px; border-radius: 5px; margin: 20px 0;">
            <h4>💡 Conseils pratiques</h4>
            <ul>
                <li>Arrivez 5-10 minutes avant votre rendez-vous</li>
                <li>En cas d'annulation, prévenez-nous au moins 2 heures à l'avance</li>
                <li>N'hésitez pas à contacter le salon pour toute question</li>
            </ul>
        </div>

        <p>Nous vous attendons avec impatience et sommes convaincus que vous passerez un excellent moment dans notre salon !</p>

        <p>Cordialement,<br>
        <strong>L'équipe {{ salon.name }}</strong></p>

        <div class="footer">
            <p>Cet email a été envoyé automatiquement par notre système de réservation.</p>
            <p>Si vous n'avez pas demandé cette réservation, veuillez nous contacter immédiatement.</p>
        </div>
    </div>
</body>
</html>
